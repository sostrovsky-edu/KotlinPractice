package syntax.functions.type.inline_func.scope_func.run.example2

/**
 * Use "run()" function if you:
 *      1. need to compute some value or want to limit the scope of multiple local variables.
 *      2. want to convert "explicit parameters" to "implicit receiver".
 */
/*
fun main() {
    val inserted: Boolean = run {
        val person5: Person = getPerson()
        val personDao: PersonDao = getPersonDao()
        personDao.insert(person5)
    }
}

fun printAge(person: Person) = person.run {
    print(age)
}*/
